# 🍽️ - onmangeou

A simple app to vote for a place to eat

## 🚀 - Quick start

```sh
git clone git@github.com:LalbaAnthony/onmangeou.git

cp .env.example .env

docker compose --profile development down ; docker compose --profile development up --build -d
```